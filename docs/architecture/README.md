# Neroa Lane Architecture Pack

This folder captures the structural extraction plan that sits on top of the
hardened canonical portal base.

Current documents:

1. [Lane Worktree Architecture v1](./lane-worktree-architecture-v1.md)

Use these docs to decide:

- which subsystem lane owns a change,
- which files remain protected shared core,
- which branch/worktree should carry the work,
- which validation must happen before a lane merges back into the protected base.
