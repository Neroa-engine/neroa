# NEROA-CX-013G - Logo-Only Viewport-Left Correction

## Scope

- Restore the desktop public header to a logo-only correction.
- Keep the navigation panel structure, size, and item layout unchanged.
- Move the existing logo farther left so it no longer covers the navigation bubble.

## Delta

- Adjusted the desktop `SiteHeader` logo anchor in `app/globals.css`.
- No navigation panel layout, sizing, color, or behavior changes.

## Impact

- Public desktop header only.
- No route, auth, or runtime behavior changes.
