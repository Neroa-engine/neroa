# NEROA-CX-013I - Header Control Reorder

## Scope

- Reorder the shared public header controls inside the existing navigation bubble.
- Keep the logo, navigation bubble shape, colors, and routing behavior unchanged.

## Delta

- Moved `Engine Board` and the primary CTA into the left-side desktop header cluster.
- Moved `Home`, the navigation picker, and the account control into the right-side desktop header cluster.

## Impact

- Shared public `SiteHeader` only.
- No page layouts or route handlers changed.
